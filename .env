# SQLite database file (relative to the prisma/ directory).
# Swap this datasource for a Postgres URL to move to a hosted DB later.
DATABASE_URL="file:./dev.db"

# Port the API server listens on (see server/index.js).
API_PORT=3001
